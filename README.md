# 🇲🇳 Fine-tuned Mongolian LLM (Llama-2-7B + LoRA + Unsloth)

**Llama-2-7B суурь загварыг монгол хэлээр instruction дагах, асуулт-хариулт зэрэг NLP даалгаварт зориулж Parameter-efficient (PEFT) аргаар LoRA болон Unsloth ашиглан fine-tune хийсэн.**

---

## Онцлог

- **Суурь загвар:** Llama-2-7B (Meta AI)
- **Сургалтын арга:** LoRA + 4-bit Quantization
- **Framework:** Unsloth (Colab optimized)
- **Dataset формат:** Alpaca (`instruction`, `input`, `output`)
- **Зорилго:** Монгол хэлний LLM-н заавар дагах болон QA чанарыг сайжруулах

---

## ⚙️ Загварын тохиргоо

```python
model_name = "meta-llama/Llama-2-7b-hf"
max_seq_length = 2048
lora_config = {
  "r": 16,
  "alpha": 16,
  "target_modules": [
    "q_proj", "k_proj", "v_proj", "o_proj",
    "gate_proj", "up_proj", "down_proj"
  ],
  "load_in_4bit": True
}
```

---

## Сургалтын параметрүүд

```python
training_args = {
  "per_device_train_batch_size": 2,
  "gradient_accumulation_steps": 4,
  "warmup_steps": 5,
  "num_train_epochs": 6,
  "learning_rate": 2e-4,
  "optimizer": "adamw_8bit",
  "weight_decay": 0.01,
  "lr_scheduler_type": "linear",
  "save_steps": 100,
  "save_total_limit": 3
}
```

## Dataset жишээ

```json
{
  "instruction": "Яриа таних технологийг хэрхэн ашиглаж болохыг 5 өгүүлбэрээр жишээлж өгөөч?",
  "input": "None",
  "output": "Дуу таних технологийг машинд гар чөлөөтэй залгах боломжийг олгохын тулд ашиглаж болно. Хэрэглэгчид зүгээр л "Жон Дөүг дуудна уу" гэж хэлэхэд машин автоматаар дуудлага холбоно. Түүнчлэн дуут тушаалыг GPS навигаци ашиглах, агаарын чанарыг тохируулах, хөгжим сонсоход ашиглаж болно. Цаашилбал, энэ нь Cortana болон Alexa зэрэг дуут туслахуудыг гэр орон даяар төхөөрөмжүүдийг удирдахад ашиглаж болно. Эцэст нь хаалга түгжээ тайлах эсвэл төлбөр зөвшөөрөхөд дуу хоолойд суурилсан биометрийн баталгаажуулалтад ашиглаж болно."
}
```

---

## Өгөгдлийн сангийн чанарын том асуудал

| Асуудал | Тайлбар |
|---|---|
| `input` = float64 | Текст байх ёстой талбарт санамсаргүй **тоо** орсон |
| Авто орчуулга | Англи→Монгол орчуулга **дүрмийн болон утгын их алдаатай** |
| Wrong facts | Түүх, газарзүй, шинжлэх ухааны **баримт худал/зөрчилтэй** |
| Mixed languages | Датад **монгол, англи, хятад** холигдсон |
| Hallucination их | Хариултууд логик ба контекст **тасарсан, хий үзэгдэлтэй** |

### Нөлөө:
```
   Суурь Llama-2-ын мэдлэг муу датад хэт overfit хийснээс  
   “Catastrophic Forgetting + Bad Overfitting” үүсэж, буруу хариулт өгдөг болсон.
```

## LLM-ын гаралт
```json

Сайн жишээ (Хэвийн хариу)
```json
{
  "input": "Хиймэл оюун ухаан яагаад чухал вэ?",
  "output": "Хиймэл оюун ухаан нь одоо байгаа технологийн дэвшил дээр өргөжүүлж, илүү нарийвчилсан, нарийн төвөгтэй асуудлуудыг шийдвэрлэх чадвартай. Хүний оюун ухааныг өөрөө мэдээлэл авах, шинжлэх, тайлбарлах, шийдвэр гаргахын тулд хиймэл оюун ухааныг ашигладаг."
}
```
Model Response:
Хиймэл оюуны талаар зөв мэдээлэл өгсөн. учир нь model-д сургасан өгөгдөлтөөс prompt өгөх үед зөв хариулна.

Муу жишээ (Буруу түүхэн факт)
```json
{
  "input": "Чингис хаан гэж хэн бэ?",
  "output": "Чингис хаан бол 13-р зууны үед Энэтхэгийн үндэсний төлөөлөгчдийн нэг байсан бөгөөд одоогийн үеийн Энэтхэгч байсан."
}
```
Энэ бол буруу мэдээлэл (fact hallucination) юм.
LoRA сургалт буруу дата дээр хэт тохирч, зөв түүхэн мэдлэгийг дарсан.

Муу жишээ (Өдөр тэмдэглэл шиг, утгагүй зохиомол өгүүллэг)

```json
{
  "input": "Чи хэн бэ?",
  "output": "Би нэг өдөр намайг зуны өдөр байсан бөгөөд үргэлж улс төрийн тогтвортой байдлыг эзэмшдэг байв. Би хувь хүн, сайхан сэтгэгдэлтэй, үргэлж тэсвэртэй хүн бөгөөд олон нийтийн мэдлэгтэй. Би надад хэцүү орчинд амьдрал дээр нь хүрч чадна гэдэгт итгэлтэй байлаа."
}
```
*Энэ бол дараах шинжтэй overfit + датаны шуугиан:
*Датанд өгүүллэг маягийн, эсвэл ярианы стильтэй хариултууд орсон.
*LoRA бага хэмжээний буруу датанд хэт тохирсон.
*Иймээс модел асуудалтай, зохиомол хариу өгч эхэлсэн.
